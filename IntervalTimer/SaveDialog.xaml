<Window x:Class="IntervalTimer.SaveDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IntervalTimer"
        mc:Ignorable="d" Background="{DynamicResource ThemeBrushKey}"
        Title="インターバルを保存" Height="100" Width="300" ResizeMode="NoResize" ShowInTaskbar="False" KeyDown="Window_KeyDown">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBox x:Name="namee"  VerticalAlignment="Center" Margin="5"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Button x:Name="OKButton" Content="OK" Grid.Column="1" Margin="5" Click="OKButton_Click"/>
            <Button x:Name="CancelButton" Content="キャンセル" Grid.Column="2" Margin="5" Click="CancelButton_Click"/>
        </Grid>
    </Grid>
</Window>
